public class class275
{

	static void method5239()
	{
		if (ItemContainer.renderOverview != null)
		{
			ItemContainer.renderOverview.method6011(class13.plane, (class138.localPlayer.x >> 7) + PendingSpawn.baseX,
			        (class138.localPlayer.y >> 7) + Game.baseY, false);
			ItemContainer.renderOverview.method6027();
		}

	}

}
